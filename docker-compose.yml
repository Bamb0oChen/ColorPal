version: "3.9"

services:
  gateway:
    build: ./gateway
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:///data/colopal.db
    volumes:
      - gateway-data:/app/data

volumes:
  gateway-data:
